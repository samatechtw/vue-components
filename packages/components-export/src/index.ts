import { STMultiselect } from '@samatech/st-multiselect'
import { STInput } from '@samatech/st-input'
import { STProgressBar } from '@samatech/st-progressbar'

window.STInput = STInput
window.STMultiselect = STMultiselect
window.STProgressBar = STProgressBar
